{
  "version": 3,
  "build": {
    "commands": ["pip install -r requirements.txt", "python app.py build"]
  },
  "framework": "python",
  "rewrites": [{ "source": "/(.*)", "destination": "app.py"}]
}
